export default interface ICardsList {
    title?: string,
    image?: string,
    link?: string
    description?: string
}

export default interface PropsChilren {
    children?: React.ReactNode[];
  }